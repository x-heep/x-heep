# Bus Type Configuration

The bus type can either be configured to `NtoM` or `onetoM`.

In a Python configuration, the bus type is set with {py:meth}`system.System.set_bus_type`, e.g. `system.set_bus_type(BusType.NtoM)` (with `from bus_type import BusType`).
